# PRD: Configuración Inicial React Native

| Metadata | Valor |
|----------|-------|
| **Versión** | 1.0 |
| **Fecha** | 2026-03-10 |
| **Estado** | Borrador |

## 🎯 Objetivo

Configurar el proyecto React Native con TypeScript, estructura de carpetas, ESLint, y todas las dependencias base.

## ⚙️ Setup

```bash
npx react-native@latest init PathyApp --template react-native-template-typescript
```

## Dependencias Base

```json
{
  "react-native": "0.74+",
  "@react-navigation/native": "^6.x",
  "@react-navigation/bottom-tabs": "^6.x",
  "@react-navigation/native-stack": "^6.x",
  "react-native-maps": "^1.x",
  "react-native-background-geolocation": "^4.x",
  "react-native-mmkv": "^2.x",
  "@tanstack/react-query": "^5.x",
  "axios": "^1.x",
  "zustand": "^4.x",
  "react-native-safe-area-context": "^4.x",
  "react-native-screens": "^3.x"
}
```

## Estructura de carpetas

```
src/
├── api/           # Axios client + endpoints
├── components/    # Componentes reutilizables
├── hooks/         # Custom hooks
├── lib/           # Utilidades (compartir con web)
├── navigation/    # React Navigation config
├── screens/       # Pantallas
├── stores/        # Zustand stores
├── theme/         # Colores, tipografía, spacing
└── types/         # TypeScript interfaces
```

## ⏱️ Tiempo Estimado

2 días

## 🔗 Dependencias

Ninguna (es el primer paso)

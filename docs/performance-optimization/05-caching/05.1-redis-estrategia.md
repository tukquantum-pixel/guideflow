# 05.1 – Redis Estrategia

| Metadata | Valor |
|----------|-------|
| Versión | 1.0 |
| Última actualización | 2026-03-08 |
| Responsable | Backend |
| Estado | Borrador |

## 📊 Qué cachear en Redis

| Dato | TTL | Patrón | Beneficio |
|------|-----|--------|-----------|
| Rutas populares | 1h | Cache-aside | Reduce queries a BD |
| Perfil de guías | 30min | Cache-aside | Consultas frecuentes |
| Resultados búsqueda | 10min | Cache-aside | Mismos filtros repetidos |
| Sesiones usuario | 7 días | Write-through | Auth rápida |
| Rate limiting | 1-15min | Directo | Seguridad |

## ⚙️ Implementación con Upstash

```typescript
import { Redis } from "@upstash/redis"

export const redis = new Redis({
  url: process.env.UPSTASH_REDIS_URL!,
  token: process.env.UPSTASH_REDIS_TOKEN!,
})

export async function getCachedOrFetch<T>(
  key: string, fetchFn: () => Promise<T>, ttl = 3600
): Promise<T> {
  const cached = await redis.get<T>(key)
  if (cached) return cached
  const data = await fetchFn()
  await redis.setex(key, ttl, JSON.stringify(data))
  return data
}
```

## 📊 Coste estimado

| Usuarios | Commands/día | Plan Upstash | Coste |
|----------|-------------|-------------|-------|
| <1k | <10k | Gratis | 0€ |
| 1k-10k | 10k-100k | Pay-as-you-go | ~5€/mes |
| 10k+ | 100k+ | Pro | ~20€/mes |
